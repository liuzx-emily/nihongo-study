# 项目说明

这是一个用于日语学习的本地 Vue 3 + Vite 网页应用。

用户请求分为两个相互独立的方向：

1. `APP`：优化网页应用，包括样式调整、修复 Bug、重构和增加功能。
2. `STUDY`：围绕日文稿进行校对、翻译、讲解、生成档案，以及回答后续日语问题。

每次收到请求时，先根据请求内容判断所属方向，再遵循对应规则。

## 平台硬性约束（最高优先级）

本项目只面向电脑端，不做任何移动端适配。以下要求为严格命令，处理任何 `APP` 请求时都必须遵守：

- 只按宽度不小于 1180px 的电脑屏幕设计、实现和验收；
- 严禁新增或保留任何专门服务于手机、平板或触控设备的代码；
- 严禁使用面向窄屏的媒体查询、移动端 viewport、触控或屏幕方向分支、安全区域变量、移动端专用组件及样式；
- 严禁在窄屏下隐藏内容、切换单栏、改变导航结构或进行其他移动端重排；
- 屏幕宽度不足时允许页面产生横向滚动，不得为适应窄屏而改变电脑端布局；
- 每次修改网页后必须检查项目中是否出现上述移动端代码；如有，必须在交付前删除；
- 除非用户明确要求修改本条平台约束，否则任何功能或样式请求都不得被解释为授权移动端适配。

## 意图判断

以下请求默认属于 `APP`：

- 修改网页、页面、布局、颜色或字体；
- 调整电脑端交互和桌面布局样式；
- 修复报错、Bug、路由或构建问题；
- 增加搜索、筛选、收藏、音频等功能；
- 修改 Vue、TypeScript、CSS 或 Vite 配置。

以下请求默认属于 `STUDY`：

- 用户发送新的日文原稿或转写稿；
- 用户发送稿件并说“处理”“新的文稿，请处理”或同等表述时，默认含义是完整处理并持久化到网页档案；
- 校正日文、翻译中文；
- 讲解单词、语法、口语、省略或句子逻辑；
- 对当前文章中的内容继续提问；
- 将学习结果生成或追加到档案；
- 用户说“继续”并要求处理稿件后续内容。

如果请求同时包含两个方向，先完成日语内容，再把结构化结果接入网页，除非用户明确指定其他顺序。

如果仅凭请求无法判断方向，优先根据用户正在谈论的对象判断：

- 谈论日语内容本身：`STUDY`；
- 谈论内容如何呈现或网页如何工作：`APP`。

不要因为用户正在浏览文章页面，就自动判断为 `APP`。当前浏览页面只能作为辅助上下文。

## APP 工作规则

处理 `APP` 请求时：

- 聚焦网页实现，不擅自修改日语原文、翻译和教学解释；
- 保持 Vue 3、Vite、TypeScript 的现有技术栈；
- 优先复用现有组件和数据结构；
- 严格遵守“平台硬性约束”，只保证电脑端可用；
- 界面必须以内容和操作为中心，不得添加纯装饰纹理、印章、超大宣传区、空洞标语、无意义动画或过量留白；
- 在不降低字号、行距和信息层级可辨识度的前提下，提高信息密度，让用户在一屏内看到尽可能多的有效学习内容；
- 标题、元数据、搜索、导航和学习内容属于有效信息；视觉层级优先使用紧凑间距、字重、颜色和分隔线表达；
- 页面调整后必须检查首屏空间利用率，并清理不承载信息或操作的界面元素与样式；
- 修改完成后运行 `npm run build`；
- 不因页面修改而覆盖已有学习档案；
- 如果需要迁移档案数据结构，确保已有档案仍能正常显示。

## STUDY 工作规则

处理 `STUDY` 请求时：

- 以帮助“N4 已通过、正在学习 N3”的用户学习日语为目标；
- 区分自动转写错误、主播口误和自然口语；
- 修正明显错误，但保留自然、即兴的播客语气；
- 无法确定的内容必须标记存疑，不得凭空补写；
- 按语意分段，每段日文原则上控制在 300～1000 字符；
- 每段包含：修正后的日文稿、完整中文翻译、适合当前阶段的进阶及重要口语词汇、进阶语法和口语结构、重点句子解析；
- 词汇和语法只记录表达、读音、释义、接续及语境说明；严禁生成、保存或展示 JLPT 等级字段和等级标签；
- 文章档案不生成、不保存也不展示主题标签或 `tags` 字段；
- 不讲解普通 N5、N4 内容，除非该表达在语境中有特殊用法；
- 长稿可以分批处理，但必须记录准确的续接位置；
- 用户只询问某个知识点时，直接回答该问题，不必重新生成整篇档案；
- 除非用户要求写入档案，否则知识点问答只在对话中回答，不修改项目文件。

## 档案持久化规则

当用户发送新的完整稿件并要求“处理”，或要求“生成档案”“加入网页”“保存这篇稿子”时：

- 在 `src/content/` 中为文章创建独立数据文件；
- 文件名和 slug 使用 `YYYY-MM-DD-英文简短名称`；
- 将文章登记到 `src/content/index.ts`；
- 不覆盖同名档案，先检查是否已存在；
- 同一稿件的后续批次应追加到原档案，而不是创建重复文章；
- 新档案必须符合现有 `StudyArticle` 类型；
- 写入后运行 `npm run build`；
- 如果用户只询问或引用某个知识点，或明确表示只需在对话中校对、解释，则不要自动写入网页。

## 当前文章与后续提问

不要在本文件中记录“当前正在学习哪篇文章”。判断当前文章时依次参考：

1. 用户在消息中明确给出的文章名称；
2. 用户引用的具体原句；
3. 当前对话最近处理的稿件；
4. `src/content/` 中最近更新且状态为“进行中”的档案。

仍然无法判断时，再询问用户指的是哪篇文章。
