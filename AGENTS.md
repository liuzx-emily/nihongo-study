# 项目与工作规范

这是一个面向日语学习的本地 Vue 3 + Vite 应用。所有请求先按目标分为 `APP` 或 `STUDY`，再执行对应规则。

## 最高原则

- 禁止擅自执行 `git push` 或以其他方式向远程仓库推送内容。
- 只有用户在当前请求中明确要求推送时，才可执行 push；提交代码、完成修改、运行构建或用户要求“发布”均不视为 push 授权。

## 请求分类

- `APP`：网页、布局、样式、交互、组件、路由、构建、搜索、筛选、收藏、音频等应用功能。
- `STUDY`：日文稿校对、中文翻译、词汇与语法讲解、学习档案生成，以及围绕文章内容的后续问答。
- 同时涉及两类目标时，先处理日语内容，再将结构化结果接入应用；用户指定顺序时从其要求。
- 无法直接分类时，以用户正在讨论的对象为准：内容本身属于 `STUDY`，呈现方式或应用行为属于 `APP`。当前浏览页面不能单独决定请求类型。

## 电脑端平台约束

应用仅为宽度不小于 1180px 的电脑屏幕设计和验收。

- 使用稳定的电脑端布局；屏幕不足 1180px 时允许横向滚动，不改变内容、栏位或导航结构。
- 项目中不得包含面向手机、平板、触控设备或窄屏的适配代码，包括窄屏媒体查询、移动端 viewport、触控或屏幕方向分支、安全区域变量，以及移动端专用组件和样式。
- 每次修改应用后检查上述限制，并在交付前移除不符合要求的代码。
- 只有用户明确要求修改本节约束时，才可改变目标平台。

## APP 规则

- 保持 Vue 3、Vite 和 TypeScript 技术栈，优先复用现有类型、组件、数据结构和样式变量。
- 应用修改不得擅自改变日文原稿、翻译或教学解释，也不得覆盖已有学习档案。
- 数据结构变更必须兼容已有档案。
- 界面以学习内容和操作为中心，在不牺牲字号、行距、可读性和信息层级的前提下提高信息密度。
- 标题、元数据、搜索、导航和学习内容均属于有效信息；使用紧凑间距、字重、颜色和分隔线建立层级。
- 不使用纯装饰纹理、印章、超大展示区、空洞标语、无意义动画或过量留白。页面调整后检查首屏空间利用率。
- 保持语义化标签及电脑端键盘操作能力。

## STUDY 规则

- 目标用户已通过 N4、正在学习 N3；重点讲解进阶表达、重要口语、语境用法和句子逻辑。
- 处理新稿、续写档案或整理语法前，必须读取根目录 `LEARNING_PROFILE.md`，并按其中的语法家族和例外规则筛选讲解项。
- 日文稿处理属于忠实校对，不是改写、缩写或总结。修正后的日文必须仍能逐句对应原稿，保留原有信息、论述顺序、例子、语气和说话人立场。
- 采用最小必要修正：只有能够从上下文可靠确认的自动转写错误、明确口误、错别字、标点或断句问题才可修改；能成立的原句一律保留，即使表达重复、绕口、不够精练或不符合书面语习惯。
- 保留自然口语中的重复、停顿、插入语、自我修正、话题跳转、语气词、犹豫和未说完的结构。不得为了流畅而删句、合并句子、调整顺序、补充逻辑、替换成概括性表达或统一成书面语。
- 只有确认是转写系统造成的无意义重复或噪声时才可删除；无法区分转写错误、说话人口误和自然口语时，保留原文并标记存疑。
- 修正不得引入原稿没有的事实、因果关系、评价或结论。若一句话需要较大改写才能变得自然，不作改写，保留原句并在讲解中说明问题。
- 无法可靠确认的内容明确标记存疑，不得补写猜测内容。
- 按语意分段，每段日文原则上为 300～1000 字符。
- 每段提供修正后的完整日文、完整中文翻译、词汇、语法与口语结构、重点句解析。
- 重点句解析优先选择理解难度高的句子，包括长难句、包含复杂语法或多层修饰的句子、存在成分省略或语序倒置的句子，以及因说话人即兴组织语言而出现指代不明、逻辑跳跃、成分衔接松散或表达不清的句子。
- 重点句数量必须按本段实际难点决定，不设固定条数。逐句检查后，凡具有独立理解难点或独立讲解价值的句子都应分别收录；不得因为已经选出一句就停止筛选，也不得为了统一格式、控制篇幅或沿用旧档案习惯而机械地每段只保留一句。
- 多个难点集中在同一句时可以合并在一条解析中；不同句子各有独立难点时不得合并或遗漏。若长稿各段的重点句数量完全一致，尤其全部都只有一句，交付前必须将其视为机械生成的异常信号并重新逐段复核。
- 对表达不清的重点句，保留原有说法，不擅自整理成更完整、更有逻辑的主张；解析中应区分语法结构与说话人的组织问题，指出具体难点，补出可由上下文可靠确认的省略成分，说明各部分可能的衔接关系，并明确标记仍无法确定之处。
- 完成每段后对照原稿复核：原稿中的每个实质信息、例子、限定、转折、语气和立场都必须在修正稿中保留；中文翻译也必须完整对应，不得用归纳或总结代替原文内容。
- 词汇和语法记录表达、读音、释义、接续及语境说明；不记录或展示 JLPT 等级。
- 普通 N5、N4 内容不作讲解，除非在当前语境中有特殊用法。
- 用户明确表示某语法“已掌握／以后不用讲”时，将其归一到 `LEARNING_PROFILE.md` 的语法家族后记录；表示“还不熟／需要复习”时移除或缩小对应覆盖范围。
- 不根据文章完成度、出现次数或过往讲解记录自动判定语法已掌握。
- 长稿可分批处理，并准确记录续接位置。
- 单个知识点问答直接在对话中完成；除非用户要求写入档案，否则不修改项目文件。

## 临时背词 Excel

- 每篇文章全文处理完成后，额外生成一份供手机背词 APP 录入的临时 Excel 表格（`.xlsx`）；长稿分批处理时，中间批次不生成，最后一批完成后汇总全文生词并去重。
- 临时背词 Excel 文件保存到电脑桌面，不在对话中重复展示完整表格，也不写入网页档案、`src/content/`、`LEARNING_PROFILE.md` 或其他项目文件。
- 生词范围沿用本文件的学习规则：面向已通过 N4、正在学习 N3 的用户，优先选择进阶、口语化及有语境价值的词汇。
- 表格固定使用 `日文｜中文翻译｜中文解释` 三列。每个生词连续输出三行，顺序固定为词典原型、原文搭配、常用搭配；中文解释中不添加用于标识这三类行的标签。
- 第一行使用词典原型，仅用于识别或调用 APP 内置词库，不受 10 字限制，也允许全部由平假名组成。
- `原文搭配` 取自原文语境。为满足录入限制，可以删除语气词、压缩结构或还原基本形，但必须保留目标词、原文中的搭配对象和原意。
- `常用搭配` 另选真实对话中高频、自然的搭配。即使原文搭配已经是最常见的表达，也必须提供另一条不同的搭配，不得重复。
- `原文搭配` 和 `常用搭配` 的日文均不得超过 10 个字符，必须包含目标词，并至少包含一个汉字或片假名；标点和符号不能代替汉字或片假名满足此条件。两条搭配都应完整、自然且便于直接记忆。
- 中文解释应具体说明目标词本身的核心含义及其在当前搭配中的作用，按实际需要补充语气、感情色彩、语域、适用场景、隐含意思或容易误解之处；不要只复述中文翻译，也不要用文章情节代替词义说明。
- 原文搭配通常仍按词语本义解释；只有目标词在原文中出现特殊语境义、比喻义或不同于常规用法的语气时，才说明该特殊之处。
- 每行中文解释控制在 50 个汉字以内，在限制内优先保证解释准确、具体且有助于区分用法；简单词不强行扩展。

以下示例仅用于展示中文解释应达到的具体程度和三行之间的信息侧重，不要求对所有词机械套用相同的解释结构：

| 日文 | 中文翻译 | 中文解释 |
| --- | --- | --- |
| 思いっきり | 尽情地；痛快地 | 把动作做到想要的最大程度，含有放下顾虑、不再克制的语气 |
| 思いっきり楽しむ | 尽情享受 | 不考虑时间或他人眼光，充分体验活动带来的快乐 |
| 思いっきり遊ぶ | 痛痛快快地玩 | 常用于假期或忙碌之后，强调暂时抛开负担、玩个尽兴 |

## 档案触发与验证

- 用户发送完整新稿并要求“处理”，等同于要求完成学习处理并保存为网页档案。
- “生成档案”“加入网页”“保存这篇稿子”等明确要求均触发持久化。
- 同一稿件的后续批次追加到原档案；创建前检查现有内容，避免重复或覆盖。
- 只引用内容、询问知识点或明确要求仅在对话中处理时，不持久化。
- 修改应用代码或档案数据后运行 `npm run build`。

## 当前文章判定

按以下顺序确定后续问题所指文章：

1. 用户明确给出的文章名称；
2. 用户引用的原句；
3. 当前对话最近处理的稿件；
4. `src/content/` 中最近更新且 `status` 为 `进行中` 的档案。

以上信息仍不足时，再询问用户。不要把当前文章名称写入指令文件。
